#!/usr/bin/env bash
# Raspberry Pi Setup Script
#

# 1. Git repo: https://github.com/GC-STEM/cs350-rpi. When downloaded to RPi, the
# repository files should be placed in the home directory (`~/`) so that the
# `cs350`, `rpilib`, and `scripts` folders are directly in the home directory.



# 2. Run